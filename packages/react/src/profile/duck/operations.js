import actions from './actions';

const {
  saveName,
  saveBio,
  saveEmail,
  saveUsername,
  savePassword,
} = actions.actions;

export default {
  saveName,
  saveBio,
  saveEmail,
  saveUsername,
  savePassword,
};
